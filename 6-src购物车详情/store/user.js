//user模块的小store
import {getUsertempId} from '@/utils/userabout'
const state = {
   userTempId:getUsertempId()
}
const mutations = {}
const actions = {}
const getters = {}
export default {
  state,
  mutations,
  actions,
  getters
}